{{#extends 'base'}}

{{#block "content"}}

<a href="/admin/products" class="mt-3 mb-3 btn btn-dark btn-sm"> Go Back </a>

<h4>Are you sure you want to delete {{product.name}}?</h4>
<form method="POST">
    <input type="hidden" name="_csrf" value="{{csrfToken}}"/>

<table class="table">
    <thead>
        <tr style="text-align:center">
            <th>ID</th>
            <th>Name</th>
            <th>Price</th>
            <th>Description</th>
            <th>Stock</th>
            <th>Quantity Sold</th>
            <th>Post Category</th>  
            <th>Image</th>
        </tr>
    </thead>
    <tbody>
        <tr style="text-align:center">
            <td>{{product.id}}</td>
            <td>{{product.name}}</td>
            <td>{{product.price}}</td>
            <td>{{product.description}}</td>
            <td>{{product.stock}}</td>
            <td>{{product.quantity_sold}}</td>
            <td>{{product.post_category.category}}</td>
            <td>
                {{#if product.thumbnail_url}}
                    <img src="{{product.thumbnail_url}}" class="img-fluid" alt="image">
                {{/if}}
            </td>
        </tr>
    </tbody>
</table>
<div class="alert alert-secondary">
    <a href="/admin/products" class="ml-2 me-4 btn btn-secondary btn-sm"> Cancel Delete </a>
    <input type="submit" class="ml-2 me-4 btn btn-dark btn-sm" value="Confirm Delete"/>
</div>
</form>
{{/block}}
{{/extends}}