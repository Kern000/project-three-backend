{{#extends 'base'}}

{{#block "content"}}

<a href="/admin/orders/{{order.order_id}}/update-order" class="mt-3 mb-3 btn btn-dark btn-sm"> Go Back </a>

<h4>Are you sure you want to delete this order item: {{order.product_name}}?</h4>
<form method="POST">
    <input type="hidden" name="_csrf" value="{{csrfToken}}"/>

<table class="table">
    <thead>
        <tr style="text-align:center">
                <th>Order ID</th>
                <th>Cart ID</th>
                <th>User ID</th>
                <th>Product ID</th>
                <th>Product Name</th>
                <th>Seller User ID</th>
                <th>Price</th>
                <th>Quantity</th>                        
                <th>Date Time</th>
                <th>Order Fulfilled</th>
        </tr>
    </thead>
    <tbody>
        <tr style="text-align:center">
            <td style="padding-top:12px;">{{order.order_id}}</td>
            <td style="padding-top:12px;">{{order.cart_id}}</td>
            <td style="padding-top:12px;">{{order.user_id}}</td>
            <td style="padding-top:12px;">{{order.product_id}}</td>
            <td style="padding-top:12px;">{{order.product_name}}</td>
            <td style="padding-top:12px;">{{order.seller_id}}</td>
            <td style="padding-top:12px;">{{order.price}}</td>
            <td style="padding-top:12px;">{{order.quantity}}</td>
            <td style="padding-top:12px;">{{order.date_time}}</td>
            <td style="padding-top:12px;">{{order.fulfilled}}</td>
        </tr>
    </tbody>
</table>
<div class="alert alert-secondary">
    <a href="/admin/orders/{{order.order_id}}/update-order" class="ml-2 me-4 btn btn-secondary btn-sm"> Cancel Delete </a>
    <input type="submit" class="ml-2 me-4 btn btn-danger btn-sm" value="Confirm Delete"/>
</div>
</form>
{{/block}}
{{#block 'js'}}
    <script>
        function goBack(){
            window.history.back();
        }
    </script>
{{/block}}
{{/extends}}